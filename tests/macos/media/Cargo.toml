[package]
name = "waterkit-media-test"
version = "0.1.0"
edition = "2021"
description = "macOS test for waterkit-media"

[[bin]]
name = "media-test"
path = "src/main.rs"

[dependencies]
waterkit-media = { path = "../../../media" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
