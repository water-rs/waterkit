[package]
name = "waterkit-dialog-test"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
waterkit-dialog = { path = "../../../dialog" }
tokio = { workspace = true, features = ["full"] }
